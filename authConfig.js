var localStrategy = require("passport-local").Strategy;

var userSchema = require("./models/users.js");

module.exports = function(passport){

  //serialize-->take a large basic object, break it into parts

console.log("reached");

}
